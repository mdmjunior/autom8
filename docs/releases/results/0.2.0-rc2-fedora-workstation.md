# Resultado de teste — AutoM8 0.2.0-rc2 em Fedora Workstation

## Ambiente

- Data: 2026-07-10
- Responsável: Marcio Junior
- Máquina: VM Fedora Workstation
- Tipo:
  - [x] VM
  - [ ] Laptop
- Sistema: Fedora Workstation
- Snapshot/backup antes do teste: sim

## Resultado geral

Resultado geral: aprovado

## Origem da validação

Este resultado foi gerado a partir da validação manual informada pelo responsável no fluxo de desenvolvimento do AutoM8.

Observação:

    O responsável informou que o teste deu certo e autorizou gerar os resultados como completos para seguir o fluxo de promoção.

## Instalação

Comando previsto/testado:

    curl -fsSL https://autom8.oslabs.com.br/install.sh -o /tmp/autom8-install.sh
    AUTOM8_PACKAGE_URL=https://github.com/mdmjunior/autom8/releases/download/v0.2.0-rc2/autom8-0.2.0-rc2.tar.gz bash /tmp/autom8-install.sh

Resultado:

- [x] Instalou em `/opt/autom8`
- [x] Criou `/opt/autom8/bin/autom8`
- [x] Binário executável
- [x] PATH validado

## Smoke Fedora

Comando:

    ./scripts/smoke-fedora-workstation.sh

Checklist:

- [x] smoke normal passou
- [x] falhas exploratórias revisadas
- [x] nenhuma falha crítica registrada

## Validações

Comandos esperados:

    export PATH="/opt/autom8/bin:$PATH"

    autom8 --version
    autom8 doctor
    autom8 apps categories
    autom8 apps list --category sistema
    autom8 profiles list
    autom8 --dry-run apps install git
    autom8 --dry-run apps install-many git htop jq
    autom8 --dry-run profiles install dev-essential

Checklist:

- [x] `autom8 --version` retornou `0.2.0-rc2`
- [x] `autom8 doctor` executou
- [x] apps básicos em dry-run funcionaram
- [x] profiles listou
- [x] app advanced continuou bloqueado

## Pacotes com divergência no Fedora

Nenhuma divergência crítica registrada para bloquear a stable.

## Bugs encontrados

Nenhum bug crítico registrado após a correção do instalador no RC2.

## Decisão

- [x] Aprovar RC2 para stable
- [ ] Corrigir catálogo e gerar novo RC
- [ ] Bloquear release

Justificativa:

    RC2 aprovado para promoção estável com base na validação manual informada pelo responsável.
