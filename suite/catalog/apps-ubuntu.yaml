apps:
  - id: git
    name: Git
    description: Sistema de controle de versão.
    category: desenvolvimento
    source: native
    distros: [ubuntu, debian]
    install: "apt install -y git"
    remove: "apt remove -y git"

  - id: curl
    name: curl
    description: Cliente para transferências HTTP e outras URLs.
    category: sistema
    source: native
    distros: [ubuntu, debian]
    install: "apt install -y curl"
    remove: "apt remove -y curl"

  - id: jq
    name: jq
    description: Processador JSON de linha de comando.
    category: sistema
    source: native
    distros: [ubuntu, debian]
    install: "apt install -y jq"
    remove: "apt remove -y jq"
